{
  "name": "@riderguy/utils",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "lint": "echo 'no lint configured'",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
