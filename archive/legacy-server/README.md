# Legacy server material

These files document the former Hetzner server and one-off wave deployments.
That server and account are no longer recoverable. The files are retained for
historical reference only and must not be run against replacement infrastructure.

Use `server-config/` and `docs/operations/NEW_SERVER_RESET.md` for the new
production baseline.
